'use strict'
const one = document.getElementById('one'),
      two = document.getElementById('two'),
      parent = document.querySelector('.parent')
console.dir(parent.firstChild)
console.dir(parent.firstElementChild)
